"""Regularized (typed, per-subsection-type) parsing layer for snl_mining
detail scraping exports. See `registry.py` for the fixed subsection-type
list and dispatch, `helpers.py` for shared extraction primitives."""
